import WrapperWithSidebar from '../wrappers/WrapperWithSidebar';

const Settings = () => {
  return (
    <WrapperWithSidebar>
      <h1>Settings</h1>
    </WrapperWithSidebar>
  );
};

export default Settings;
